import React, { Component } from 'react';
import Button from 'react-bootstrap/Button';

export default class ButtonStart extends Component {
  render() {
    return (
        <Button variant="warning">Start Sorting</Button>
    );
  }
}
